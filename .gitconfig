[user]
        name = Craig Andera
        email = candera@wangdera.com
[color]
        ui = auto
[push]
	default = matching
[core]
	excludesfile = /Users/candera/.gitignore_global
[github]
	user = candera
[hub]
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[credential]
	helper = store
